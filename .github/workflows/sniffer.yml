name: Sniffer Cron
on:
  schedule:
    - cron: '*/2 * * * *'   # 매 2분 (UTC)
  workflow_dispatch:        # 필요하면 수동 실행 버튼
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Call Supabase function
        run: curl -sS "https://fgamdcaijqkkioafnqga.functions.supabase.co/sniffer-recent"
